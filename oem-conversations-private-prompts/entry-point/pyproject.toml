[project]
name = "wanderlust-demo"
version = "0.1.0"
description = "Demo script for Wanderlust private prompts example"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
