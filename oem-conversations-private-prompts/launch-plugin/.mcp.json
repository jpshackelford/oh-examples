{
  "mcpServers": {
    "wanderlust": {
      "url": "${MCP_SERVER_URL}/mcp",
      "transport": "streamable-http",
      "headers": {
        "Authorization": "Bearer ${MCP_AUTH_TOKEN}"
      }
    }
  }
}
